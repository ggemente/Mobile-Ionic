import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agenda-telefonica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
